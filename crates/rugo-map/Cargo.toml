[package]
name = "rugo-map"
description = "The sharded, SIMD-probed cache table at the centre of rugo."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

[dependencies]
rugo-arena.workspace = true
rugo-hash.workspace = true

[target.'cfg(loom)'.dependencies]
loom.workspace = true

[dev-dependencies]
criterion.workspace = true
# The memory test's thresholds are written against the arena's own constants, so that moving one means restating the other.
rugo-arena.workspace = true

[[bench]]
name = "map"
harness = false

[lints]
workspace = true

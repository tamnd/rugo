[package]
name = "rugo-arena"
description = "The per-shard slab allocator rugo stores entry bytes in."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

# A segment is an anonymous mapping, so that the part of it nothing has been written to costs address space and not memory. See `src/segment.rs`.
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[lints]
workspace = true

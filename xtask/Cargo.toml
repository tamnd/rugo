[package]
name = "xtask"
description = "Repository chores that have to be identical on every machine. Not published."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
publish = false

[dependencies]
# The sweep files come out of cache-bench, which is a different repository on a different release cycle. They are read as values rather than into structs on purpose: a field added there should not stop the scoreboard rendering here.
serde_json = "1"

[lints]
workspace = true
